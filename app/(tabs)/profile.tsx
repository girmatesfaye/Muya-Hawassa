import React from "react";
import { TouchableOpacity } from "react-native";

export default function profile() {
  return (
    <div>
      <TouchableOpacity>
        <h1>Profile</h1>
      </TouchableOpacity>
    </div>
  );
}
