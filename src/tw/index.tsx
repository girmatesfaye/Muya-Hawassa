/**
 * NativeWind v4 component re-exports.
 * With NativeWind v4, the Babel transform automatically adds `className`
 * support to all React Native core components — no wrappers needed.
 */
export {
  View,
  Text,
  ScrollView,
  Pressable,
  TextInput,
  TouchableHighlight,
  TouchableOpacity,
  FlatList,
  Image,
  SafeAreaView,
} from "react-native";

export { Link } from "expo-router";
